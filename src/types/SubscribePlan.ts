export interface ISubscribePlan {
  title: string;
  subtitle: string;
  price: string;
  btnText: string;
  features: string[];
  icon: string;
}

export interface ISubscribePlanData {
  title: string;
  subtitle: string;
  price: number;
}
